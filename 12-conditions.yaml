- name: condition
  hosts: local
  connection: local
  vars:
    number: 12
  tasks:
  - name: check number is less then 10 or not
  ansible.buitin.debug:
     msg: "given number {{ number }} is less then 10"
     when: number <10
  