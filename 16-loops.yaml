- name: loops
  hosts: local
  connection: local
  become: yes
  gather_facts: no

  tasks:
    - name: install packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql
        - zip
