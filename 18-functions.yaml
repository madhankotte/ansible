- name: functions
  hosts: local
  connection: local
  vars:
    technologies: "Devops, Linux, shell, ansible"  # string
    tags:
      application: payment
      environment: dev
    listtags:
      - key: application
        value: payment
      - key: environment
        value: dev
  tasks:
    - name: default variable
      ansible.builtin.debug:
        msg: "Hello, {{ name | default('madhan') }}"

    - name: convert string to list
      ansible.builtin.debug:
        msg: "Technologies are: {{ technologies | split(', ') }}"

    - name: convert map to list
      ansible.builtin.debug:
        msg: "Tags are: {{ tags | dict2items }}"

    - name: convert list to map
      ansible.builtin.debug:
        msg: "Tags are: {{ listtags | items2dict }}"

    - name: read yaml and take it into variable
      ansible.builtin.debug:
        course.yaml: "{{ lookup('file', 'course.yaml') | from_yaml }}"

    - name: convert yaml into json
      ansible.builtin.debug:
        msg: "json: {{ course.yaml | }}"
